# Gunicorn configuration file
# Configures the server settings for production deployment

# Timeout for worker processes (in seconds).
# Set to 240 seconds to allow processing of very large PDF/Word files.
timeout = 240
